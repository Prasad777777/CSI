# Use a Module
# Now we can use the module we just created, by using the import statement:

# Now we will import the module we have created which is named - my_module.py

import my_module

my_module.greetings("Ram")
